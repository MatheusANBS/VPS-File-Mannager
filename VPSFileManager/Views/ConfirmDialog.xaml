<ui:FluentWindow x:Class="VPSFileManager.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Confirm"
        Height="240" Width="450"
        MinHeight="200" MinWidth="400"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="None"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        ResizeMode="NoResize">

    <Border Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Icon and Title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                <ui:SymbolIcon x:Name="IconSymbol" Symbol="Warning24" FontSize="24" Margin="0,0,12,0" Foreground="#F7B547"/>
                <TextBlock x:Name="TitleText" Text="Confirmation Required" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Message -->
            <TextBlock x:Name="MessageText" Grid.Row="1" Text="Are you sure you want to proceed?" 
                       FontSize="13" Opacity="0.8" TextWrapping="Wrap" Margin="0,0,0,20"/>

            <!-- Buttons -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,0">
                <ui:Button x:Name="CancelButton" Content="Cancel" Appearance="Transparent" Padding="24,8" Click="Cancel_Click" MinWidth="100" Margin="0,0,8,0"/>
                <ui:Button x:Name="ConfirmButton" Content="Confirm" Appearance="Caution" Padding="24,8" Click="Confirm_Click" MinWidth="100"/>
            </StackPanel>
        </Grid>
    </Border>
</ui:FluentWindow>
