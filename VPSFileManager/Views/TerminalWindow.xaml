<ui:FluentWindow x:Class="VPSFileManager.Views.TerminalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:controls="clr-namespace:VPSFileManager.Controls"
        Title="Terminal — SSH" 
        Height="650" Width="1000"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="None"
        Background="#1E1E1E">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Title="Terminal — SSH" Grid.Row="0" Background="#1E1E1E" Foreground="#CCCCCC"/>

        <!-- Terminal Control -->
        <Border Grid.Row="1" Background="#1E1E1E" Padding="4,2,4,2">
            <controls:TerminalControl x:Name="TerminalView" 
                                       Focusable="True"
                                       IsEnabled="True"/>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#252526" Padding="12,6" 
                BorderBrush="#3C3C3C" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Status de conexão -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse x:Name="StatusDot" Width="8" Height="8" Fill="#4EC9B0" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="StatusText" Text="Connected" Foreground="#808080" FontSize="11" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Info da conexão -->
                <TextBlock Grid.Column="1" x:Name="ConnectionLabel" Text="" Foreground="#606060" FontSize="11" 
                           VerticalAlignment="Center" HorizontalAlignment="Center"/>

                <!-- Tamanho do terminal -->
                <TextBlock Grid.Column="2" x:Name="SizeLabel" Text="120x30" Foreground="#606060" FontSize="11" 
                           VerticalAlignment="Center" Margin="20,0,0,0"/>

                <!-- Separador -->
                <Rectangle Grid.Column="3" Width="1" Fill="#3C3C3C" Margin="12,2"/>

                <!-- Botões de ação -->
                <StackPanel Grid.Column="4" Orientation="Horizontal">
                    <Button x:Name="ClearBtn" Content="Clear" Click="ClearBtn_Click"
                            Background="Transparent" Foreground="#808080" BorderThickness="0"
                            Padding="8,2" FontSize="11" Cursor="Hand"/>
                    <Button x:Name="CopyBtn" Content="Copy All" Click="CopyBtn_Click"
                            Background="Transparent" Foreground="#808080" BorderThickness="0"
                            Padding="8,2" FontSize="11" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
