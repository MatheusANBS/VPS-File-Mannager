<ui:FluentWindow x:Class="VPSFileManager.Views.FolderPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Select Folder"
        Height="500" Width="600"
        MinHeight="400" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="None"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Title="Select Destination Folder" Grid.Row="0"/>

        <!-- Main Content -->
        <Border Grid.Row="1" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Description -->
                <StackPanel Grid.Row="0" Margin="0,0,0,16">
                    <TextBlock x:Name="DescriptionText" Text="Select where to save the downloaded files" FontSize="13" Opacity="0.8" TextWrapping="Wrap"/>
                </StackPanel>

                <!-- Current Path -->
                <Border Grid.Row="1" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="6" Padding="12,8" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="CurrentPathText" Text="C:\" FontFamily="Consolas" FontSize="12" VerticalAlignment="Center"/>
                        <ui:Button Grid.Column="1" Icon="{ui:SymbolIcon FolderOpen24}" Content="Browse..." Click="Browse_Click" Appearance="Transparent" Padding="8,4"/>
                    </Grid>
                </Border>

                <!-- Special Folders -->
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="QUICK ACCESS" FontSize="10" FontWeight="Bold" Opacity="0.5" Margin="4,0,0,8"/>
                        
                        <ui:Button x:Name="DesktopButton" Icon="{ui:SymbolIcon Desktop24}" Content="Desktop" Appearance="Transparent" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,0,0,4" Click="SpecialFolder_Click" Tag="Desktop"/>
                        <ui:Button x:Name="DocumentsButton" Icon="{ui:SymbolIcon Document24}" Content="Documents" Appearance="Transparent" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,0,0,4" Click="SpecialFolder_Click" Tag="MyDocuments"/>
                        <ui:Button x:Name="DownloadsButton" Icon="{ui:SymbolIcon ArrowDownload24}" Content="Downloads" Appearance="Transparent" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,0,0,4" Click="SpecialFolder_Click" Tag="Downloads"/>
                        <ui:Button x:Name="MusicButton" Icon="{ui:SymbolIcon MusicNote124}" Content="Music" Appearance="Transparent" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,0,0,4" Click="SpecialFolder_Click" Tag="MyMusic"/>
                        <ui:Button x:Name="PicturesButton" Icon="{ui:SymbolIcon Image24}" Content="Pictures" Appearance="Transparent" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,0,0,4" Click="SpecialFolder_Click" Tag="MyPictures"/>
                        <ui:Button x:Name="VideosButton" Icon="{ui:SymbolIcon Video24}" Content="Videos" Appearance="Transparent" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,0,0,4" Click="SpecialFolder_Click" Tag="MyVideos"/>
                    </StackPanel>
                </ScrollViewer>

                <!-- Buttons -->
                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <ui:Button Content="Cancel" Appearance="Transparent" Padding="24,8" Click="Cancel_Click" MinWidth="100" Margin="0,0,8,0"/>
                    <ui:Button Content="Select" Appearance="Primary" Padding="24,8" Click="Select_Click" MinWidth="100"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
