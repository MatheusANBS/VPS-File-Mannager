<ui:FluentWindow x:Class="VPSFileManager.Views.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Atalhos de Teclado - VPS File Manager"
        Height="600" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Border Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <StackPanel Margin="0,0,0,20">
            <TextBlock Text="Atalhos de Teclado" FontSize="28" FontWeight="Bold"/>
            <TextBlock Text="Dicas rápidas para usar o VPS File Manager" FontSize="13" Opacity="0.7" Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Shortcuts List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ShortcutsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="16" Margin="0,0,0,12" Background="{DynamicResource CardBackgroundBrush}" 
                                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" 
                                CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="180"/>
                                </Grid.ColumnDefinitions>

                                <!-- Shortcut Keys -->
                                <StackPanel>
                                    <TextBlock FontFamily="Consolas" FontSize="13" FontWeight="SemiBold" 
                                               Foreground="{DynamicResource SystemAccentColorPrimaryBrush}">
                                        <Run Text="{Binding Keys}"/>
                                    </TextBlock>
                                </StackPanel>

                                <!-- Action Description -->
                                <StackPanel Grid.Column="1" Margin="20,0,0,0">
                                    <TextBlock Text="{Binding Action}" FontWeight="SemiBold" FontSize="13"/>
                                    <TextBlock Text="{Binding Description}" FontSize="11" Opacity="0.6" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                </StackPanel>

                                <!-- Context -->
                                <Border Grid.Column="2" Padding="10,4" Background="{DynamicResource ControlFillColorDefaultBrush}" 
                                        CornerRadius="4" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding Context}" FontSize="11" Opacity="0.7"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Close Button -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <ui:Button Content="Fechar" Click="Close_Click" Appearance="Primary" Padding="32,10"/>
        </StackPanel>
        </Grid>
    </Border>
</ui:FluentWindow>
