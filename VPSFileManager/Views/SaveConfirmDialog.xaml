<ui:FluentWindow x:Class="VPSFileManager.Views.SaveConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Unsaved Changes"
        Height="220" Width="460"
        MinHeight="200" MinWidth="400"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="None"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        ResizeMode="NoResize">

    <Border Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Icon and Title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                <ui:SymbolIcon Symbol="DocumentEdit24" FontSize="24" Margin="0,0,12,0" Foreground="#F7B547"/>
                <TextBlock Text="Unsaved Changes" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Message -->
            <TextBlock x:Name="MessageText" Grid.Row="1"
                       Text="Do you want to save changes before closing?"
                       FontSize="13" Opacity="0.8" TextWrapping="Wrap" Margin="0,0,0,24"/>

            <!-- Buttons: Cancel | Don't Save | Save -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Cancel" Appearance="Transparent" Padding="20,8"
                           Click="Cancel_Click" MinWidth="90" Margin="0,0,8,0"/>
                <ui:Button Content="Don't Save" Appearance="Secondary" Padding="20,8"
                           Click="DontSave_Click" MinWidth="100" Margin="0,0,8,0"/>
                <ui:Button Content="Save" Appearance="Primary" Padding="20,8"
                           Click="Save_Click" MinWidth="90"
                           Icon="{ui:SymbolIcon Save24}"/>
            </StackPanel>
        </Grid>
    </Border>
</ui:FluentWindow>
